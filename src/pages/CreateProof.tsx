import React from 'react';
import { CreateProofModule } from '../components/CreateProofModule';

const CreateProof: React.FC = () => {
  return (
    <div className="min-h-screen bg-bg py-8">
      <CreateProofModule />
    </div>
  );
};

export default CreateProof;
